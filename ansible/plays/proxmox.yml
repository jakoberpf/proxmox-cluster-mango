---
- name: Preprare the development proxmox with basic settings, packages and networking
  hosts: development

  roles:
    - role: hypervisor/proxmox
      vars:
        pve_reboot_on_kernel_update: false
